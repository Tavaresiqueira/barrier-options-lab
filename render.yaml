services:
  - type: web
    name: barrier-options-lab
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: DJANGO_DEBUG
        value: "false"
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: MAX_MONTE_CARLO_PATHS
        value: "12000"
